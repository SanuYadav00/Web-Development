let str="apple";
if (str[0]==='a' && str.length>3) {
 console.log(`The given string ${str} is a good string.`); 
}
 else {
  console.log(`The given string ${str} is not a good string.`); 
 }
 let str2="sanu";
 if (str2[0]==='a' && str2.length>3) {
  console.log(`The given string ${str2} is a good string.`); 
 }
  else {
   console.log(`The given string ${str2} is not a good string.`); 
  }
  let str3="app";
  if (str3[0]==='a' && str3.length>3) {
   console.log(`The given string ${str3} is a good string.`); 
  }
   else {
    console.log(`The given string ${str3} is not a good string.`); 
   }
   let str4="sa";
   if (str4[0]==='a' && str4.length>3) {
    console.log(`The given string ${str4} is a good string.`); 
   }
    else {
     console.log(`The given string ${str4} is not a good string.`); 
    }
      